# Tell the user project where to find our headers and libraries
set(@TARGET_SET@_INCLUDE_DIR  "@TARGETSET_INCLUDE_DIR@")
set(@TARGET_SET@_INCLUDE_DIRS "${@TARGET_SET@_INCLUDE_DIR}")
#set(@TARGET_SET@_LIBRARY_DIRS "@TARGETSET_LIB_DIR@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("@TARGETSET_CMAKE_DIR@/@TARGET_SET@-targets.cmake")

# Defines (if required)
#set(@TARGET_SET@_DEFINITIONS "@TARGETSET_COMPILE_DEFINITIONS@")

# USE file (if there is one)
#set(@TARGET_SET@_USE_FILE "@TARGETSET_CMAKE_DIR@/@TARGET_SET@-use.cmake")